services:
  sase-5g-manager:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: prisma-5g-sase-manager
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
